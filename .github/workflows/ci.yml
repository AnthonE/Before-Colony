name: CI

on:
  push:
  pull_request:

env:
  CARGO_TERM_COLOR: always

jobs:
  rust:
    name: fmt, clippy, tests, no-alloc, determinism
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      # rust-toolchain.toml pins the compiler, clippy, rustfmt and the wasm32 target.
      - run: rustup show
      - uses: Swatinem/rust-cache@v2
      - name: Format
        run: cargo fmt --all -- --check
      - name: Clippy (hot-path bans are errors)
        run: cargo clippy --workspace --all-targets -- -D warnings
      - name: Tests, including the zero-allocation and determinism proofs (release for speed)
        run: cargo test --workspace --release
      - name: Tick benchmark smoke run
        run: cargo bench -p bc-sim --bench tick -- --warm-up-time 1 --measurement-time 2

  wasm:
    name: browser client + wasm determinism
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: rustup show
      - uses: Swatinem/rust-cache@v2
      - name: Install wasm-bindgen-cli (must match the crate)
        run: cargo install wasm-bindgen-cli --version 0.2.128 --locked
      - name: Clippy (wasm client)
        run: cargo clippy -p bc-client --target wasm32-unknown-unknown -- -D warnings
      - name: Clippy (wasm client, WebGPU build)
        run: cargo clippy -p bc-client --target wasm32-unknown-unknown --features webgpu -- -D warnings
      - name: Determinism on wasm32 (same golden hash as native)
        run: cargo test -p bc-sim --target wasm32-unknown-unknown --release --test determinism
      - name: Build the web client
        run: ./scripts/build-web.sh webgl2
